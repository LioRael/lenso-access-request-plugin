{
  "$schema": "https://json-schema.org/draft/2020-12/schema", "type": "object", "additionalProperties": false,
  "required": ["organization_id", "request_id", "effect_id", "expected_effect_revision", "resolution", "evidence_reference", "idempotency_key"],
  "properties": {
    "organization_id": { "type": "string", "minLength": 1, "maxLength": 240 },
    "request_id": { "type": "string", "minLength": 1, "maxLength": 160 },
    "effect_id": { "type": "string", "minLength": 1, "maxLength": 160 },
    "expected_effect_revision": { "type": "string", "pattern": "^[1-9][0-9]*$" },
    "resolution": { "type": "string", "enum": ["succeeded", "failed"] },
    "evidence_reference": { "type": "string", "minLength": 1, "maxLength": 1000 },
    "idempotency_key": { "type": "string", "minLength": 1, "maxLength": 200 }
  }
}
